version: "3.9"

services:
  forwarder:
    build: .
    container_name: telegram-forwarder
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
